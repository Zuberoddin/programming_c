#include<stdio.h>
main()
{
int arr[6]={2,7,4,7,8,3,4};
int i,j,num;
printf("enter the repeating element\n");
scanf("%d",&num);
for(i=0;i<=6,i++)
{
	for(j=i+1;j<i;j++)
	{
		if(arr[i]==arr[j])
		{
			continue;
		}
	}
}
printf("%d\n",arr[i]);
