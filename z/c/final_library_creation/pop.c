#include"myheader.h"
void pop()
{
	int j=0,arr[SIZE];
        if(j==-1)
        {
                printf("stack is underflow\n");
        }
        else
        {
                printf("poped element is %d\n",arr[j]);
                j=j-1;
        }
}

